# Frontend (Vite) — optional legal / open-source links
VITE_GITHUB_REPO=https://github.com/your-org/alpha-vision-insights
VITE_CONTACT_EMAIL=legal@example.com

# API base URL (defaults to /api in production Docker)
# VITE_API_URL=http://localhost:8000

# Backend — see README and docker-compose.yml for data updater, DeepVaR, reload token
# INTERNAL_RELOAD_TOKEN=
# HF_TOKEN=
# OPENAI_API_KEY=
